import 'package:flutter/material.dart';

class AppColors {
  // Y2K / Kirby inspired palette
  static const primary = Color(0xFFFF69B4); // Hot Pink
  static const secondary = Color(0xFF00D9FF); // Cyber Blue
  static const accent = Color(0xFFFFF066); // Sunshine Yellow
  static const success = Color(0xFF00FF94); // Neon Green
  static const warning = Color(0xFFFF6B00); // Orange
  static const error = Color(0xFFFF0066); // Hot Red

  // Backgrounds
  static const bgPrimary = Color(0xFFFFF5F7); // Soft Pink
  static const bgSecondary = Color(0xFFE6F7FF); // Soft Blue
  static const bgDark = Color(0xFF1A0B2E); // Deep Purple

  // UI Elements
  static const cardBg = Colors.white;
  static const cardShadow = Color(0x33000000);

  // Text
  static const textPrimary = Color(0xFF2D1B4E);
  static const textSecondary = Color(0xFF7B68A6);
  static const textLight = Colors.white;

  // Game specific
  static const star = Color(0xFFFFD700);
  static const coin = Color(0xFFFFA500);
  static const health = Color(0xFFFF4444);
  static const energy = Color(0xFF44FF44);

  // Gradient colors for mascots and special effects
  static const gradientStart = Color(0xFFFF6EC7);
  static const gradientEnd = Color(0xFF7B68EE);

  // Level colors
  static const levelBeginner = Color(0xFF90EE90);
  static const levelIntermediate = Color(0xFFFFD700);
  static const levelAdvanced = Color(0xFFFF6347);
  static const levelExpert = Color(0xFF9370DB);
}
